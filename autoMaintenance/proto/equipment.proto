syntax = "proto3";

package grpc;

option go_package = "autoMaintenance/proto";

service Scheduler {
    rpc ScheduleMaintenance(Equipment) returns (Response);
}

message Equipment {
    string name = 1;
}

message Response {
    string status = 1;
}